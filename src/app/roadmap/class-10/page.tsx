import { ComingSoon } from '@/components/ui';
import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Class 10' };

export default function Page() {
  return <ComingSoon section="Class 10" />;
}
