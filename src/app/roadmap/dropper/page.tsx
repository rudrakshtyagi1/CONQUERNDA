import { ComingSoon } from '@/components/ui';
import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Dropper' };

export default function Page() {
  return <ComingSoon section="Dropper" />;
}
