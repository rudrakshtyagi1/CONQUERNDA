import { ComingSoon } from '@/components/ui';
import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Cutoff Analysis' };

export default function Page() {
  return <ComingSoon section="Cutoff Analysis" />;
}
