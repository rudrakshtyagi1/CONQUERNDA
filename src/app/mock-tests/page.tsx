import { ComingSoon } from '@/components/ui';
import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Mock Tests' };

export default function Page() {
  return <ComingSoon section="Mock Tests" />;
}
