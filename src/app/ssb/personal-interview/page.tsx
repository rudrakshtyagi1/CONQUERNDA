import { ComingSoon } from '@/components/ui';
import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Personal Interview' };

export default function Page() {
  return <ComingSoon section="Personal Interview" />;
}
