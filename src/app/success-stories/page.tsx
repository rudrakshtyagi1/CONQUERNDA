import { ComingSoon } from '@/components/ui';
import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Success Stories' };

export default function Page() {
  return <ComingSoon section="Success Stories" />;
}
