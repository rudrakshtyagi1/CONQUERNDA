import { ComingSoon } from '@/components/ui';
import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Fitness' };

export default function Page() {
  return <ComingSoon section="Fitness" />;
}
