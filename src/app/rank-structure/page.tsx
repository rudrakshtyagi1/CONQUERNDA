import { ComingSoon } from '@/components/ui';
import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Rank Structure' };

export default function Page() {
  return <ComingSoon section="Rank Structure" />;
}
